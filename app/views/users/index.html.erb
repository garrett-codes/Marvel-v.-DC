<!DOCTYPE html>
<html>
  <h1> Choose your player: </h1> 
<body>
  <ul> 
    <% @users.each do |user| %>
      <li> <%= link_to user.name, user_path(user) %> </li>
    <% end %>
  </ul>
  <%= button_to "Create a new player", new_user_path, method: :get%> 
</body>
</html>